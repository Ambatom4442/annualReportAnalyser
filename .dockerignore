# =============================================================================
# Docker Ignore - Exclude only essential files from Docker build context
# =============================================================================

# Virtual environment (use container's own)
.venv/
venv/
ENV/
env/

# Environment file with secrets (pass via env vars instead)
.env

# Git
.git/

# Python cache
__pycache__/
*.py[cod]
*$py.class

# Local data (will be mounted or recreated in container)
.data/

# OS files
.DS_Store
Thumbs.db
